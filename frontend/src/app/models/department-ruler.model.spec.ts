import { GobernanteDepartamento } from './department-ruler.model';

describe('DepartmentRuler', () => {
  it('should create an instance', () => {
    expect(new GobernanteDepartamento()).toBeTruthy();
  });
});
