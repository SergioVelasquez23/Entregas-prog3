import { MachinerySpeciality } from './machinery-speciality.model';

describe('MachinerySpeciality', () => {
  it('should create an instance', () => {
    expect(new MachinerySpeciality()).toBeTruthy();
  });
});
