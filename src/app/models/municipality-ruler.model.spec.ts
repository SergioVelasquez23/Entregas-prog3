import { MunicipalityRuler } from './municipality-ruler.model';

describe('MunicipalityRuler', () => {
  it('should create an instance', () => {
    expect(new MunicipalityRuler()).toBeTruthy();
  });
});
